#ifndef BITS
#include <bits/stdc++.h>
#define BITS
#endif
#ifndef UTILIO
#define UTILIO
#include "util_io.h"
#endif

using namespace std;

string paction(void) {
   string ret;
   cout << "> " << flush;
   cin >> ret;

   return ret;
}

