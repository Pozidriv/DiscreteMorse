#ifndef _BITS
#include <bits/stdc++.h>
#define _BITS
#endif
#ifndef _UTIL_IO
#define _UTIL_IO
#include "util_io.h"
#endif

using namespace std;

string paction(void) {
   string ret;
   cout << "> " << flush;
   cin >> ret;

   return ret;
}

